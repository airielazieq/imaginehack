import Placeholder from './Placeholder'

// TODO: Replaced by the scenario trigger / stream controls in a later task (Requirement 19).
export default function MockController() {
  return <Placeholder title="Mock Controller" subtitle="Scenario triggers + stream control land here." />
}
