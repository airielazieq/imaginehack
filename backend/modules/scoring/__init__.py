"""Scoring engine package (cross-cutting).

- :mod:`priority_scorer` computes the 6-factor weighted Priority Score (0-100)
  that drives the composite heatmap (task 7.1).
- :mod:`dimension_scorer` computes per-dimension scores for the matrix heatmap
  (task 7.2).
"""
